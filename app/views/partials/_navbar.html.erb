<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">HomeChef</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <%= link_to "Home", root_path, class: "nav-link"%>

      <li class="nav-item">
      <%= link_to "Become A Chef", new_chef_profile_path, method:"GET", class:"nav-link"%>
      </li>
      
      <li class="nav-item active">
      <% if user_signed_in?%>
        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link"%>
      <% else %>
        <%= link_to "Log In", new_user_session_path, class: "nav-link"%>
      <% end %>
      </li>
      
      <% if user_signed_in?%>
      <% if current_user.profile %>
      <%= link_to " #{current_user.profile.first_name}", profile_path(current_user.profile.id), class:"nav-link" %>
      <% elsif current_user.chef_profile %>
      <%= link_to " Chef#{current_user.chef_profile.first_name}", chef_profile_path(current_user.chef_profile.id), class:"nav-link" %>
      <% else %>
      <%end%>
      <%end%>
      
      
      </li>
       
      
      
      

    </ul>
  </div>
</nav>

 